# 10_Smart-Photo-Organizer Agent 開發規範

本專案遵循目前有效之全域開發憲法；本檔僅定義專案專屬規則與例外。

---

## 1. 專案核心流程與階段式工作流 (UI/UX 規範)
- **五階段標準操作流程**：
  1. **選擇來源**：選擇本機照片資料夾或 Google Takeout ZIP 壓縮檔。
  2. **分析與分類**：比對雜湊、EXIF 時間、Sidecar JSON 與 MediaGroup。
  3. **問題確認**：檢視疑似重複、模糊/連拍、日期缺失或待審核項目。
  4. **預覽整理結果**：清楚呈現目錄結構、目標路徑與變更摘要。
  5. **正式執行**：確認後才進行檔案搬移、隔離或歸檔。
- **重要安全與透明性原則**：
  - **分析階段唯讀保證**：分析與審核階段**嚴禁搬移、重新命名或刪除任何原始照片/影片**。
  - **狀態提示**：UI 介面必須醒目標註「目前處於分析預覽模式，尚未修改任何原始檔案」。
  - **大量處理預覽防線**：在大規模實體搬移或隔離前，必須提供變更預覽與明確確認機制。

---

## 2. 業務領域與資料防禦特別約束
- **Takeout ZIP 直讀保護**：
  - 來源 Google Takeout ZIP 壓縮檔一律視為**永久唯讀**，不修改、不更名、不刪除來源 ZIP。
  - 維持 ZIP 直讀與單檔 `.part` 流水線，嚴禁退回全量解壓模式。
  - ZIP 成員不直接建立實體捷徑，僅被列入審核之 MediaGroup 依需快取至 `_ReviewCache`。
- **權威資料來源與 MediaGroup 完整性**：
  - 本機 SQLite 資料庫為斷點續傳、MediaGroup 配對與審核狀態的唯一權威來源。
  - Windows `.lnk` 捷徑僅供輔助瀏覽，嚴禁以捷徑路徑取代資料庫核對。
  - **配對檔不可拆散**：照片/影片本體、Google `.json` Sidecar 與 Live Photo 動態配對檔必須整組處理，絕不可分離遺漏。
- **隔離防線取代永久刪除**：
  - 系統內部不提供永久刪除功能；`99_待刪除` 僅觸發安全搬移至 `_Quarantine/待刪除`，由使用者於 Windows 資源回收筒或檔案總管手動確認清空。

---

## 3. 多 Agent 協作與審查停止條件
- **文件順序**：`AGENTS.md` → `HANDOFF.md` → `tasks.md` → `spec.md` → `ARCHITECTURE.md` → `plan.md` → `DESIGN.md` → `MEMORY.md`。
- **交接與審查停止條件**：
  - 輪替開發遵循共用單一 `HANDOFF.md`，不建立平台專屬交接檔。
  - 當 `tasks.md` 目標達成、無 Blocking 問題、測試全數通過且無重大資料風險時，即判定可交付並結束審查，嚴禁兩 Agent 無限互相發散重構。

---

## 4. 核心驗證方式
- 修改資料模型、分類演算法或檔案操作管線後，必須執行完整自動化測試：
  ```bash
  pytest tests/ -q
  ```
