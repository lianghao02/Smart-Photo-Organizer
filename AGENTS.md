# 10_Smart-Photo-Organizer Agent 開發規範

本專案遵循目前有效之全域開發憲法；本檔僅定義專案專屬規則與例外。

---

## 1. 專案核心流程與檔案隔離邊界 (UI/UX 規範)
- **分析審核與實質操作嚴格隔離**：
  - 分析／審核階段必須與實際檔案搬移、重組或刪除階段清楚隔離。
  - **分析階段唯讀保證**：分析與檢視階段**嚴禁修改、重新命名、搬移或刪除任何原始照片與影片**。
  - **狀態提示透明性**：UI 介面必須明確讓使用者知悉「目前處於分析預覽模式，尚未修改任何原始檔案」。
  - **大量處理預覽防線**：在大規模實體變更前，必須提供變更預覽與明確的確認機制。

---

## 2. 業務領域與資料防禦特別約束
- **來源壓縮檔唯讀保護**：
  - 來源 Google Takeout 等封存 ZIP 壓縮檔一律視為**永久唯讀**，嚴禁修改、更名或就地刪除來源。
  - 維持串流/單檔流水線讀取，嚴禁退回全量無條件就地解壓模式。
- **權威資料來源與媒體群組 (MediaGroup) 完整性**：
  - 本機持久化資料庫為斷點續傳、分組配對與審核狀態的唯一權威來源；作業系統捷徑僅供輔助瀏覽，嚴禁作為唯一狀態依據。
  - **配對檔不可拆散**：照片/影片本體、中繼 Sidecar JSON 與動態照片配對檔必須維持同一生命週期整組處理，絕不可分離遺漏。
- **隔離防線取代永久刪除**：
  - 系統內部不提供永久直接刪除功能；排除項目應先安全移至可復原之本機隔離區域，由使用者於作業系統端確認清理。

---

## 3. 多 Agent 協作與審查停止條件
- **文件順序**：`AGENTS.md` → `HANDOFF.md` → `tasks.md` → `spec.md` → `ARCHITECTURE.md` → `plan.md` → `DESIGN.md` → `MEMORY.md`。
- **交接與審查停止條件**：
  - 輪替開發遵循共用單一 `HANDOFF.md`，不建立平台專屬交接檔。
  - 當 `tasks.md` 目標達成、無 Blocking 問題、測試全數通過且無重大資料風險時，即判定可交付並結束審查，嚴禁兩 Agent 無限互相發散重構。

---

## 4. 核心驗證方式
- 修改資料模型、分類演算法或檔案操作管線後，必須執行完整自動化測試：
  ```bash
  pytest tests/ -q
  ```
