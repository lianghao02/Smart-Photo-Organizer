# Smart-Photo-Organizer AI 協作規則

本檔是本專案範圍內唯一的 AI 協作規則來源；上層全域憲法與平台安全規範仍優先適用。Antigravity、Codex 與其他開發代理均須先閱讀本檔，再依 `tasks.md` 執行當前任務。

## 一、文件閱讀順序

1. `AGENTS.md`：角色、權限與共同規則。
2. `tasks.md`：目前唯一有效的工作單與驗收條件。
3. `spec.md`：v3.0 產品需求與不可破壞的行為。
4. `ARCHITECTURE.md`：現況、目標模組與資料流。
5. `plan.md`：階段順序與進入下一階段的條件。
6. `DESIGN.md`：UI／UX 規格。
7. `MEMORY.md`：已知坑洞、外部借鏡與歷史決策。
8. `CHANGELOG.md`：已發布與尚未發布的變更。

若文件互相衝突，依上述順序處理；若仍無法判斷，停止可能造成資料損失的修改並回報使用者。

## 二、角色分工

### Antigravity：主要開發者

負責：

- 讀取現有程式後分析需求與根因。
- 依 `tasks.md` 實作功能、跨檔案整合與 UI。
- 補齊必要測試，執行測試並檢查 Git 差異。
- 完成一個可驗收階段後提交 Git，交由 Codex 複查。

除非遇到重大架構衝突或資料安全疑慮，原則上依「分析 → 實作 → 測試 → 檢查差異」完成工作，不只交付計畫。

### Codex：程式碼審查與除錯

負責：

- 優先檢查 `tasks.md`、最新 Git 差異或指定 commit。
- 依序檢查資料遺失風險、功能錯誤、Regression、邊界條件、錯誤處理、相容性、效能與維護性。
- 必須自行執行適用測試，不以開發報告中的「全部通過」代替驗證。
- 小範圍問題可在使用者或 `tasks.md` 授權下精準修正。
- 涉及資料模型、核心流程或大量跨檔案重構時，只提出 Blocking 問題、影響與建議方案，不直接大改。

審查結果分為：

- `Blocking`：資料風險、主流程無法運作或驗收條件未達成，必須先修。
- `Important`：可能造成明顯錯誤或日後難以恢復，建議本階段修正。
- `Minor`：非必要改善，不得阻擋階段完成。

## 三、共同工程規則

1. 修改前先閱讀現有程式，不可依舊對話猜測架構。
2. 優先最小修改，保留與任務無關且可運作的功能。
3. 不任意增加 Framework、伺服器、套件或建置系統。
4. Windows 10、台灣繁體中文、中文檔名／路徑與離線使用為主要環境。
5. 高風險搬移、隔離或刪除流程必須先有 `DRY_RUN`，且預設不得永久刪除。
6. Google Takeout ZIP 永遠唯讀，不修改、不改名、不刪除來源 ZIP。
7. SQLite 是續傳、MediaGroup、審核項目與處理狀態的權威資料；`.lnk` 只供使用者瀏覽，不可作為唯一狀態來源。
8. 不可信任捷徑的目標路徑；處理前須核對資料庫紀錄、允許的根目錄與 MediaGroup。
9. 不可拆散媒體、Google JSON Sidecar 或 Live Photo 配對檔。
10. 不碰觸、覆寫或提交使用者既有的未提交變更，除非 `tasks.md` 明確列入範圍。
11. 修改後至少執行語法載入、相關單元測試與 Git 差異檢查。
12. Commit 格式：`Type: 台灣繁體中文簡述`。

## 四、專案特別約束

- 既有 Takeout ZIP 直讀與單檔 `.part` 流水線是 v3.0 的匯入基礎，不得為了建立「完整解壓工作區」而退回全量解壓。
- v3.0 採「先分析／人工審核，最後才處理原檔」；現有直接歸檔功能在替代流程驗證完成前須保留。
- ZIP 成員無法直接成為 Windows `.lnk` 目標。只有被列入審核的 MediaGroup 才按需實體化至 `_ReviewCache`，未列入審核者不得長期占用額外空間。
- `01`～`06` 審核分類只建立捷徑；只有 `_Review/99_待刪除` 可觸發整組搬移至 `_Quarantine/待刪除`。
- 系統不提供永久刪除；使用者自行於 Windows 刪除隔離區。

## 五、Git 協作與停止條件

標準流程：

`使用者更新 tasks.md → Antigravity 實作與測試 → Commit → Codex Review → 必要的小修 → 測試 → Final Commit`

禁止 Antigravity 與 Codex 無限互相審查。完成下列條件即可結束本階段：

- `tasks.md` 驗收條件全部達成。
- 無已知 Blocking 問題。
- 相關測試實際通過。
- Git 差異只包含本次授權範圍。

