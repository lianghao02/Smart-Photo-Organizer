# Antigravity 專案指引

先讀取根目錄 `AGENTS.md`、`docs/tasks.md`、`docs/spec.md` 與 `docs/DEVELOPMENT_RULES.md`。

- 負責功能實作、跨檔案整合與必要測試。
- 不修改 `.agents/`；不複製或同步 Codex 指引。
- 完成變更後執行 `scripts/qa.ps1`，提交可審查的分支。
- 使用 `ag/dev` Worktree；不得與 Codex 共用工作資料夾。
