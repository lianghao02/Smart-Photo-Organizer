# Smart-Photo-Organizer v3.0 UI／UX 規格

## 一、設計原則

- 面向非技術使用者，畫面以任務流程與安全狀態為主，不顯示內部 Phase 名稱。
- 預設深色介面可保留，但按鈕名稱須直接描述結果。
- 高風險操作必須有預覽、目標位置、處理組數與不可復原範圍說明。
- UI 第一階段不重寫；等 v3.0 Phase 1～9 核心流程穩定後，Phase 10 才完成最終整理。

## 二、最終主要操作

1. `選擇來源`：一般資料夾、單一 Takeout ZIP 或 ZIP 所在資料夾。
2. `開始分析`：只建立索引、MediaGroup 與審核結果，不動來源原檔。
3. `開啟審核資料夾`：開啟 `_Review`。
4. `處理待刪除`：先顯示 DRY_RUN 摘要，再把完整 MediaGroup 移至 `_Quarantine/待刪除`。
5. `開啟隔離區`：開啟 `_Quarantine/待刪除`，不提供永久刪除。
6. `依日期整理`：將審核後 MediaGroup 整組歸檔。

## 三、狀態資訊

至少顯示：

- 掃描來源與有效 ZIP 數。
- 已分析／總 MediaGroup 數。
- 各審核分類數量。
- `_ReviewCache` 已使用容量。
- 未解決錯誤與可續傳狀態。
- 目前是否為 DRY_RUN。

## 四、Phase 10 移除或調整

- 移除 OneDrive／Google Drive 雲端防下載選項與說明。
- 一般模式不再以 Copy／Move 作為主要第一層選擇，改成「分析 → 人工審核 → 隔離／歸檔」。
- 將截圖、模糊與日期異常文字改為「列入審核」，不可寫成「自動移動」。
- 保留停止、暫停、日誌、系統列縮小與開啟資料夾功能。

## 五、錯誤訊息

- 使用者可理解的主訊息在前，技術細節可展開檢視。
- 清楚區分來源 ZIP 損毀、Sidecar 衝突、媒體解析失敗、磁碟空間不足與捷徑驗證失敗。
- 不使用「100% 無瑕疵」等無法驗證的宣稱；以實際測試數、失敗數與未解決項目呈現。

